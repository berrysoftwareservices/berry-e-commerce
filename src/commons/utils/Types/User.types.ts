export type userDataType = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
};

export type userRegisteredType = {
  id: string;
  email: string;
};

export type UserIdType = {
  id: string;
};
